#include "Window.h"

namespace Hazel {

   WindowProps::WindowProps(const std::string& title /*= "Hazel Engine"*/, unsigned int width /*= 1280*/, unsigned int height /*= 720*/) : Title(title), Width(width), Height(height) {

   }

}